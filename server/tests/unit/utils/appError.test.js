const AppError = require("../../../utils/appError");

describe("AppError Utility", () => {
  it("should create an error with the correct status code and message", () => {
    // Test will go here
  });

  it("should set the status property based on the status code", () => {
    // Test will go here
  });

  it("should store the original error stack trace", () => {
    // Test will go here
  });

  it('should handle 400-level status codes as "fail"', () => {
    // Test will go here
  });

  it('should handle 500-level status codes as "error"', () => {
    // Test will go here
  });
});
